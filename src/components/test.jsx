const addToInput = (value) => {
	if (this.state.input !== 0) {
		let valueArr = [...this.state.input];

		if (
			valueArr[valueArr.length + 1] !== value &&
			typeof val !== 'number'
		) {
			return null;
		} else {
			this.setState({ input: this.state.input + value });
		}
	}
};

const addToInput = (value) => {
	if (this.state.input !== 0) {
		let valueArr = [...this.state.input];
		if (
			valueArr[valueArr.length + 1] !== value &&
			typeof val !== 'number'
		) {
		}
		//popracawac nad tym
	}

	this.setState({ input: this.state.input + value });
};
